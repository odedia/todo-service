---
applications:
  - name: todo-service
    memory: 1G
    path: target/app.jar
    services:
     - mysql
    env:
      JBP_CONFIG_OPEN_JDK_JRE: '{ jre: { version: 11.+ }}'
      WAVEFRONT_PROXY_URL: proxy://<replace-with-your-proxy-route!>
